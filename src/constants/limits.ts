export const VIDEO_LENGTH_SECONDS_LIMIT = 15; // 15 sec is the maximum allowed for a video upload (in seconds)
export const IMAGE_SIZE_LIMIT_MB = 5; // 5 MB limit for image upload (this is the limit from the AI tool)
export const VIDEO_SIZE_LIMIT_MB = 20; // 20 MB limit for image upload
export const MAX_FREE_SCANS = 3;
